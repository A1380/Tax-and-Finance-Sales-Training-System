import os
from pathlib import Path

# 定义项目根目录
BASE_DIR = Path("sales-ai-coach")

# 定义所有需要创建的文件路径和内容
FILES = {
    # 1. 项目章程
    BASE_DIR / ".specify" / "memory" / "constitution.md": """# 项目章程 —— SalesAI Coach

## 1.1 项目名称
**SalesAI Coach —— 财税行业销售AI陪练系统**

## 1.2 项目愿景
为财税服务行业的销售与客服团队，提供一个 **7×24小时、零风险、可量化** 的AI销售陪练平台。通过AI驱动的虚拟客户（Agent）与智能评估体系，帮助销售人员快速提升话术能力、异议处理技巧和客户沟通水平，将传统需要3-6个月的销售实战经验积累周期大幅缩短。

## 1.3 核心原则
1. **场景真实**：所有训练场景必须基于财税行业真实业务场景（财税咨询、短视频获客、客户异议处理等）
2. **反馈即时**：每次对话结束后，系统必须在3秒内给出结构化评分和改进建议
3. **数据安全**：所有对话数据和用户信息必须加密存储，符合财税行业合规要求
4. **可量化评估**：每个训练 session 必须产出可量化的评分报告，支持培训效果追踪
5. **规范驱动开发**：所有开发工作严格遵循 SpecKit 规范流程，先写规范、后写代码

## 1.4 文档语言
所有规范文档、计划文档、任务文档、代码注释和提交信息，**必须使用简体中文（zh-CN）** 。
""",

    # 2. 功能规范
    BASE_DIR / "specs" / "001-sales-coach" / "spec.md": """# 功能规范 —— SalesAI Coach

## 2.1 功能概述
**销售AI陪练系统**是一个基于 **Agentic RAG** 架构的智能销售训练平台。系统通过多智能体协作（AI客户Agent + AI教练Agent + AI评估Agent），为销售人员提供沉浸式的实战演练环境。

## 2.2 用户角色
| 角色 | 描述 |
|------|------|
| 学员（Sales Rep） | 接受培训的销售人员，进行对话演练 |
| 培训管理员（Admin） | 管理训练剧本、查看学员数据、配置系统 |
| 系统（AI Agent） | 自动扮演客户、教练、评估员 |

## 2.3 用户故事（User Stories）

### US-01：作为学员，我希望与AI虚拟客户进行语音/文字对话演练
- **场景**：学员选择训练场景（如"财税咨询冷 call"、"异议处理"），AI扮演虚拟客户进行对话
- **验收标准**：
  - 系统支持至少3种预设训练场景（冷 call、异议处理、需求挖掘）
  - AI客户能够基于场景设定做出合理、拟真的回应
  - 对话支持文字输入（MVP）和语音输入（进阶）
  - 单次对话时长不限，可随时结束

### US-02：作为学员，我希望在每次演练后获得即时、结构化的反馈
- **场景**：对话结束后，系统自动生成包含多维度评分的反馈报告
- **验收标准**：
  - 反馈维度至少包括：开场白质量、需求挖掘能力、异议处理能力、沟通节奏把控、结尾收网能力
  - 每个维度给出具体评分（1-10分）和改进建议
  - 反馈报告以可视化图表呈现，支持导出
  - 反馈生成时间不超过5秒

### US-03：作为学员，我希望查看我的历史训练记录和进步轨迹
- **场景**：学员登录后可以查看所有历史对话记录和评分趋势
- **验收标准**：
  - 系统保存完整的对话历史
  - 提供评分趋势图，展示各维度能力的进步曲线
  - 支持按日期、场景类型筛选历史记录

### US-04：作为培训管理员，我希望能够创建和配置训练剧本
- **场景**：管理员通过自然语言描述创建新的训练场景
- **验收标准**：
  - 支持通过自然语言描述生成训练剧本（AI辅助生成）
  - 剧本配置包括：客户角色设定、场景背景、潜在异议列表、评分标准
  - 支持剧本的启用/停用和版本管理

### US-05：作为培训管理员，我希望查看团队整体的训练数据
- **场景**：管理员查看所有学员的训练完成情况、平均分、能力短板分析
- **验收标准**：
  - 提供团队维度的数据看板
  - 支持按部门、岗位、时间段筛选数据
  - 可导出团队训练报告（Excel/PDF）

## 2.4 功能需求（Functional Requirements）

| ID | 需求描述 | 优先级 |
|----|----------|--------|
| FR-01 | 系统必须支持用户注册、登录和身份认证 | P0 |
| FR-02 | 系统必须提供至少3种预设训练场景（财税冷 call、异议处理、需求挖掘） | P0 |
| FR-03 | 系统必须集成LLM（如GPT-4/Gemini）驱动AI客户进行拟真对话 | P0 |
| FR-04 | 系统必须在每次对话结束后自动生成多维度评分报告 | P0 |
| FR-05 | 系统必须保存完整的对话历史，支持学员回溯查看 | P1 |
| FR-06 | 系统必须支持管理员创建、编辑、管理训练剧本 | P1 |
| FR-07 | 系统必须提供团队维度的训练数据看板 | P2 |
| FR-08 | 系统必须支持对话内容的专业合规校验（财税行业标准） | P1 |
| FR-09 | 系统必须支持RAG知识库检索，确保AI回复内容基于财税专业知识 | P1 |
| FR-10 | 系统必须支持语音输入与语音合成（进阶功能，MVP可先做文字版） | P2 |

## 2.5 非功能需求（Non-Functional Requirements）

| ID | 需求描述 | 指标 |
|----|----------|------|
| NFR-01 | API响应时间 | < 3秒 |
| NFR-02 | 系统可用性 | ≥ 99.5% |
| NFR-03 | 对话数据加密存储 | AES-256 |
| NFR-04 | 支持并发用户数 | ≥ 50人同时训练 |
| NFR-05 | 系统架构可扩展 | 支持后续增加语音、多模态功能 |
""",

    # 3. 技术方案
    BASE_DIR / "specs" / "001-sales-coach" / "plan.md": """# 技术方案 —— SalesAI Coach

## 3.1 技术选型

### 后端
| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 编程语言 | Python 3.12+ | AI生态最成熟 |
| Web框架 | FastAPI | 高性能、异步支持、自动API文档 |
| LLM集成 | OpenAI API / 兼容接口 | 支持GPT-4、GLM等 |
| Agent框架 | LangChain / LangGraph | 多智能体协作编排 |
| 向量数据库 | Chroma / Pinecone | RAG知识库检索 |
| 数据库 | PostgreSQL + JSONB | 用户数据、对话记录、评分存储 |

### 前端
| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 框架 | React 18 + Next.js 14 | 成熟的UI框架 |
| 样式 | Tailwind CSS | 快速UI开发 |
| 状态管理 | Zustand / React Context | 轻量级状态管理 |
| API调用 | React Query / Axios | 数据请求与缓存 |

### 基础设施
| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 部署 | Docker + Nginx | 容器化部署 |
| 版本控制 | Git + GitHub | 代码托管 |
| CI/CD | GitHub Actions | 自动化测试与部署 |

## 3.2 系统架构
┌─────────────────────────────────────────────────────────────────┐
│ 前端（React + Next.js） │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│ │ 登录/注册 │ │ 场景选择 │ │ 对话界面 │ │ 评分报告 │ │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
└─────────────────────────────┬───────────────────────────────────┘
│ REST API / WebSocket
┌─────────────────────────────▼───────────────────────────────────┐
│ 后端（FastAPI + Python） │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ API Gateway 层 │ │
│ │ /auth /scenarios /chat /feedback /analytics │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Agent 编排层（LangGraph） │ │
│ │ ┌──────────┐ ┌──────────┐ ┌──────────┐ │ │
│ │ │ 客户Agent │ │ 教练Agent │ │ 评估Agent │ │ │
│ │ └──────────┘ └──────────┘ └──────────┘ │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ RAG 检索层 │ │
│ │ ┌──────────┐ ┌──────────┐ ┌──────────┐ │ │
│ │ │ 向量检索 │ │ 知识库 │ │ 合规校验 │ │ │
│ │ └──────────┘ └──────────┘ └──────────┘ │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────────┘
│
┌─────────────────────────────▼───────────────────────────────────┐
│ 数据层 │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│ │PostgreSQL│ │ 向量数据库│ │ LLM │ │
│ │(业务数据) │ │ (知识库) │ │ (API) │ │
│ └──────────┘ └──────────┘ └──────────┘ │
└─────────────────────────────────────────────────────────────────┘

## 3.3 参考开源项目
以下开源项目可作为开发参考：

| 项目 | 说明 | 借鉴点 |
|------|------|--------|
| [AI Sales Trainer (Python)](https://github.com/KaiNiskanen/python_ai_sales_trainer) | AI销售训练MVP | API架构、对话流程、反馈系统 |
| [Sparrow AI Partner Catalyst](https://github.com/brn-mwai/sparrow-ai-partner-catalyst) | AI销售陪练 | 场景设计、评分维度、实时反馈 |
| [RevenueCoach AI](https://github.com/manynames3/revenue-coach-ai) | 销售通话教练 | 评分卡设计、音频处理、JSONB存储 |
| [Virtual Sales Agent](https://github.com/lucasboscatti/sales-ai-agent-langgraph) | LangGraph虚拟销售Agent | Agent编排、工具调用、人机协同 |
| [Azure AI Sales Coach](https://github.com/Azure-Samples/voicelive-api-salescoach) | Azure语音销售教练 | 语音对话、实时分析 |
""",

    # 4. 任务拆解
    BASE_DIR / "specs" / "001-sales-coach" / "tasks.md": """# 任务拆解 —— SalesAI Coach

## 4.1 Phase 1：项目初始化与基础设施（Week 1）

| 任务ID | 任务描述 | 产出 |
|--------|----------|------|
| T001 | 初始化Git仓库，配置SpecKit项目结构 | `.specify/` 目录、`spec.md`、`plan.md` |
| T002 | 搭建FastAPI后端项目骨架 | `backend/` 目录、基础路由、Docker配置 |
| T003 | 搭建React + Next.js前端项目骨架 | `frontend/` 目录、基础页面、路由配置 |
| T004 | 配置PostgreSQL数据库，设计数据模型 | 用户表、对话记录表、评分表、剧本表 |
| T005 | 配置向量数据库（Chroma），初始化财税知识库 | 知识库向量索引 |

## 4.2 Phase 2：核心对话功能（Week 2-3）

| 任务ID | 任务描述 | 产出 |
|--------|----------|------|
| T006 | 实现用户认证模块（注册/登录/JWT） | `/api/auth` 接口 |
| T007 | 集成LLM API（OpenAI/Gemini），实现基础对话能力 | `/api/chat` 接口 |
| T008 | 基于LangGraph实现多Agent编排（客户Agent + 教练Agent） | Agent工作流 |
| T009 | 实现RAG知识库检索，注入财税专业知识 | RAG检索模块 |
| T010 | 实现前端对话界面（聊天窗口、消息渲染） | 对话UI组件 |
| T011 | 实现3个预设训练场景（冷 call、异议处理、需求挖掘） | 场景配置文件 |

## 4.3 Phase 3：评估与反馈系统（Week 4）

| 任务ID | 任务描述 | 产出 |
|--------|----------|------|
| T012 | 实现对话结束后的自动评分逻辑 | 评分引擎 |
| T013 | 设计并实现多维度评分卡（开场/挖掘/异议/节奏/收网） | 评分卡模板 |
| T014 | 实现评分报告生成与可视化展示 | 报告页面 |
| T015 | 实现对话历史保存与查询 | 历史记录API |
| T016 | 实现学员个人训练数据看板（评分趋势、能力雷达图） | 个人看板 |

## 4.4 Phase 4：管理后台与优化（Week 5）

| 任务ID | 任务描述 | 产出 |
|--------|----------|------|
| T017 | 实现管理员剧本管理功能（创建/编辑/删除） | 剧本管理界面 |
| T018 | 实现AI辅助生成训练剧本（自然语言→剧本配置） | 剧本生成器 |
| T019 | 实现团队数据看板（学员列表、平均分、完成率） | 团队看板 |
| T020 | 实现对话内容的财税合规校验 | 合规校验模块 |
| T021 | 性能优化与测试（API响应时间<3秒） | 性能测试报告 |

## 4.5 Phase 5：部署上线（Week 6）

| 任务ID | 任务描述 | 产出 |
|--------|----------|------|
| T022 | 配置Docker Compose，实现一键本地部署 | `docker-compose.yml` |
| T023 | 配置GitHub Actions CI/CD流水线 | `.github/workflows/` |
| T024 | 编写项目README、API文档、部署文档 | 文档 |
| T025 | 上传代码到GitHub仓库，打v1.0.0标签 | GitHub仓库 |
""",

    # 5. 质量检查清单
    BASE_DIR / "specs" / "001-sales-coach" / "checklists" / "requirements.md": """# 质量检查清单 —— SalesAI Coach

## 5.1 需求质量检查

- [ ] CHK001：所有用户故事是否有明确的验收标准？
- [ ] CHK002：功能需求是否都分配了优先级（P0/P1/P2）？
- [ ] CHK003：非功能需求是否包含可量化的指标？
- [ ] CHK004：是否覆盖了边缘情况（如网络中断、AI服务不可用）？
- [ ] CHK005：是否考虑了财税行业的专业合规要求？

## 5.2 技术方案检查

- [ ] CHK006：技术选型是否与团队能力匹配？
- [ ] CHK007：系统架构图是否清晰展示了各层职责？
- [ ] CHK008：是否考虑了系统的可扩展性？
- [ ] CHK009：是否参考了已有的开源实现？

## 5.3 任务拆解检查

- [ ] CHK010：任务是否按阶段合理拆分？
- [ ] CHK011：每个任务是否有明确的产出？
- [ ] CHK012：任务依赖关系是否清晰？
- [ ] CHK013：工时估算是否合理？
""",

    # 6. 项目 README
    BASE_DIR / "README.md": """# SalesAI Coach —— 财税行业销售AI陪练系统

本项目是一个基于 **Agentic RAG** 架构的智能销售训练平台，为财税服务行业的销售与客服团队提供沉浸式实战演练环境。

## 🎯 项目背景
基于某财税科技公司的AI业务场景落地需求，聚焦销售培训痛点，通过AI Agent模拟真实客户，实现零风险、可量化的销售实战演练。

## 🛠 技术栈
- **后端**：Python 3.12+ / FastAPI / LangGraph / PostgreSQL / Chroma
- **前端**：React 18 / Next.js 14 / Tailwind CSS
- **部署**：Docker / GitHub Actions

## 📂 SpecKit 规范文档
本项目的需求、方案、任务均按照 SpecKit 规范组织，文件结构如下：
sales-ai-coach/
├── .specify/memory/
│ └── constitution.md # 项目章程（愿景与原则）
└── specs/001-sales-coach/
├── spec.md # 功能规范（用户故事与需求）
├── plan.md # 技术方案（架构与选型）
├── tasks.md # 任务拆解（6周开发计划）
└── checklists/
└── requirements.md # 质量检查清单

## 🚀 快速开始（开发计划）
1. **Phase 1**：初始化项目结构（Week 1）
2. **Phase 2**：实现核心对话功能（Week 2-3）
3. **Phase 3**：构建评估与反馈系统（Week 4）
4. **Phase 4**：开发管理后台（Week 5）
5. **Phase 5**：部署与上线（Week 6）

详细任务请查看 `specs/001-sales-coach/tasks.md`。

## 📦 参考开源项目
- [AI Sales Trainer](https://github.com/KaiNiskanen/python_ai_sales_trainer)
- [Sparrow AI Partner Catalyst](https://github.com/brn-mwai/sparrow-ai-partner-catalyst)
- [RevenueCoach AI](https://github.com/manynames3/revenue-coach-ai)
- [Virtual Sales Agent](https://github.com/lucasboscatti/sales-ai-agent-langgraph)

## 📄 许可证
待定
"""
}

def create_project():
    """创建所有目录和文件"""
    for file_path, content in FILES.items():
        # 确保父目录存在
        file_path.parent.mkdir(parents=True, exist_ok=True)
        
        # 写入文件
        with open(file_path, 'w', encoding='utf-8') as f:
            f.write(content)
        
        print(f"✅ 已生成: {file_path}")

    print("\n🎉 所有 SpecKit 项目文件已生成成功！")
    print(f"📁 项目根目录: {BASE_DIR.absolute()}")
    print("\n下一步：")
    print("1. cd sales-ai-coach")
    print("2. git init && git add . && git commit -m 'docs: 初始化 SpecKit 规范'")
    print("3. 按照 tasks.md 开始 Phase 1 开发")

if __name__ == "__main__":
    create_project()